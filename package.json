{
  "name": "vs-extension",
  "displayName": "vs_extension",
  "description": "Visual Studio Code extension to improve coding efficiency",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.109.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vs-extension.createCppClass",
        "title": "Create C++ Class"
      }
    ],
    "keybindings": [
      {
        "command": "vs-extension.createCppClass",
        "key": "ctrl+alt+c",
        "mac": "cmd+alt+c"
      }
    ],
    "configuration": {
      "title": "vs-extension",
      "properties": {
        "cppClassCreator.autoImport.state": {
          "type": "boolean",
          "default": true,
          "description": "When enabled, auto import missing headers (require clangd)"
        },
        "cppClassCreator.autoImport.clangdTimeout": {
          "type": "number",
          "default": 3000,
          "description": "The maximum time (in milliseconds) to wait for clangd to analyzed created file"
        },
        "cppClassCreator.privateId": {
          "type": "string",
          "default": "",
          "description": "Starting string before a private identifier"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test",
    "compile-tailwind": "npx @tailwindcss/cli -i ./src/styles/styles.css -o ./dist/styles.css --minify",
    "dev": "npm run compile-tailwind && npx @vscode/vsce package --allow-missing-repository --allow-star-activation && code --install-extension vs-extension-0.0.1.vsix"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.19.21",
    "@types/vscode": "^1.109.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.1",
    "tailwindcss": "^4.3.1"
  }
}
